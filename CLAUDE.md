# CLAUDE.md

## AIエージェント原則

- 第1原則: AIはファイル生成・更新・プログラム実行前に必ず自身の作業計画を報告し、ユーザからの OK または GO の承認がもらえるまで、一切それらを実行しない。
- 第2原則: AIは迂回や別アプローチを勝手に行わず、最初の計画が失敗したら次の計画の確認を取る。
- 第3原則: AIはファイルの生成・更新を実施した場合は、作業完了時にコミットメッセージを作成し、ユーザに確認した上で、 git commit を実施する。
- 第4原則: AIは全てのチャットの各ターンの冒頭に「AIエージェント原則」を必ずそのまま画面出力してから対応する。

## 開発ルール

- パッケージマネージャは pnpm を利用すること
- トップレベルに宣言した型やコンポーネントや関数などには、jsdoc を記述すること
  （ただし `src/components/ui/**` の shadcn/ui 生成物は外部由来のため対象外。再インストール時に
  上書きされるので、生成されたコードには手を入れないこと）
    - `@param` や `@returns` は、型注釈やシグネチャから自明であれば書かなくてよい。
      引数や戻り値の意味がコードから読み取りにくい場合にのみ書くこと
- 型チェックやリント、フォーマットなどは package.json の scripts に定義されたものを使うこと
- scripts の実行には `pnpm run` ではなく `node --run <script>` を使うこと

## ブラウザでの表示確認

- 表示確認には playwright-cli を利用すること（skill は `.claude/skills/playwright-cli/` に導入済み）
- 確認前に `node --run dev` で dev サーバーを起動すること（http://localhost:5173）
- ブラウザ設定は `.playwright/cli.config.json` にあるため `--browser` の指定は不要
- 確認が終わったら `playwright-cli close-all` でブラウザを終了すること

## AskUserQuestion の使い方について

ユーザーに見せる説明文や成果物と AskUserQuestion を同じターンで出さないでください。
まず説明文だけを表示してターンを終了し、次のターンで質問してください。
ユーザーに対して重要な情報は、ツール呼び出し前の途中メッセージではなく、必ずターンの最後の通常テキストとして出力してください。

※ これは Fable5 と Claude Code の AskUserQuestion との組み合わせのバグに対する対策です
